server:
  port: 8083

spring:
  application:
    name: alumnos-service
  datasource:
    url: jdbc:mysql://localhost:3306/alumnos-api-felix?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

# Configuración JWT
jwt:
  secret: "miClaveSecretaMuySeguraYFijaParaJWT12345678901234567890"

# Logs para debugging de seguridad
logging:
  level:
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    com.peral.alumnos: DEBUG